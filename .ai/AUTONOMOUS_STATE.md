# Autonomous State

- Current roadmap version: v1
- Current task ID: AUTO-012
- Current task status: DONE
- Current branch: main
- Last run timestamp: 2026-08-03T00:48:02Z
- Last successful commit hash: 290a4bb (updater repoint committed); this state.md update pending its own commit
- Latest run summary: AUTO-012 complete — Forgejo release pipeline and repointed update feed. Built a full `.forgejo/workflows/release.yml` (build-windows/build-linux/release jobs) from scratch, iterating through eight real tag-push test runs to find and fix each real-world failure: `dtolnay/rust-toolchain` not on Forgejo's default action mirror (fixed with a full GitHub URL), `actions/setup-node` failing under the Windows service context (dropped — Node already present on both runners), Alpine's default docker image lacking bash/apt-get (pinned `container: node:20-bookworm` explicitly), a genuine OOM-kill on `hetzner-1`/fmrdigital (relocated the Linux runner to a new `scooby-docker` runner on Scooby, which has far more headroom), missing `xdg-utils` for AppImage bundling, NSIS bundling failing specifically under the `LocalSystem` service account (fixed by the user setting the service's logon to a real Administrator account — Claude never touched the password), `upload-artifact@v4`/`download-artifact@v4` not supported on this Forgejo version (downgraded to v3), and finally — the biggest structural finding — that `fmrdigital/featherMD` can't be made truly public because the `fmrdigital` org itself is private and caps all member repos to "internal" regardless of individual repo settings. Solved by publishing releases to a new, separate, genuinely public repo under the personal account, `frank/featherMD-releases` (binaries + `latest.json` only, never source), authenticated with a new purpose-scoped token. Since this Forgejo version has no `/releases/latest/download/` alias, the pipeline publishes to both the real version tag and a fixed `latest` tag (`override: true`) to give the updater a stable URL. Final test run (`v1.10.5-fmr-pipeline-test8`) succeeded fully on the first attempt after all fixes landed; anonymous (unauthenticated) fetch of both `latest.json` and the Windows installer confirmed via curl. `tauri.conf.json`'s `updater.endpoints` and CSP `connect-src` now point at the real, working feed.
- Files changed in the latest run: `.forgejo/workflows/release.yml` (new), `scripts/build-latest-json.mjs` (new — merges per-platform Tauri updater signatures into one manifest, since a plain `tauri build` invocation has no equivalent to `tauri-action`'s `updaterJsonKeepUniversal`), `src-tauri/tauri.conf.json` (`updater.endpoints`, CSP `connect-src`), `.ai/DECISIONS.md` (three new entries covering the LocalSystem/NSIS fix, the runner relocation, and the private-org/public-releases-repo finding). External (non-repo) changes: new Forgejo runners (`optimus-windows` on Optimus via NSSM service, `scooby-docker` on Scooby via systemd), `ForgejoRunner` Windows service logon changed to a real Administrator account, `has_actions` flipped on for `fmrdigital/featherMD`, new public repo `frank/featherMD-releases`, two new Forgejo Action secrets (`TAURI_SIGNING_PRIVATE_KEY`/`_PASSWORD` on `fmrdigital/featherMD`, `RELEASES_REPO_TOKEN` also on `fmrdigital/featherMD`).
- Validation commands and results: `npm run lint` — 0 errors/warnings. `npm run test` — 22 files, 379 tests, all passed (no regression from any of this session's changes). Real end-to-end pipeline validation via 8 real tag pushes against the live Forgejo instance (not simulated) — the final one succeeded completely: signed Windows `.exe` + Linux `.deb`/`.AppImage`, merged `latest.json`, all anonymously fetchable. `forge diff-check --root .` not re-run this cycle (no src-tauri/policy-relevant path changes beyond the pre-approved `tauri.conf.json` updater/CSP fields).
- Current blockers: None. AUTO-012 is fully done.
- Known risks and assumptions: The `ForgejoRunner` Windows service now runs as a real Administrator account rather than `LocalSystem` — a deliberate trade-off (see `.ai/DECISIONS.md`) to fix NSIS bundling; slightly larger blast radius if that service were ever compromised. Optimus and Scooby are both now load-bearing for releases (must be on/reachable). `frank/featherMD-releases`' `latest` release/tag gets overwritten (`override: true`) on every tag push — by design, but means its content is always "whatever was built most recently," not a stable historical artifact (the per-version tag releases serve that purpose instead).
- Recommended next task: The tabs feature (AUTO-015/016/017) and fork-identity/release-pipeline (AUTO-011/012) — the two big pieces of the original fork motivation — are both now complete. Remaining roadmap: AUTO-004 (fs:scope permission audit), AUTO-006 (render-token/LRU cache test coverage), AUTO-007 (Windows-only cfg gating cross-platform check), AUTO-008 (broader jsdom DOM-API gap audit) — all lower-priority risk-audit tasks. Awaiting user direction on which to tackle next, or whether to cut a first real (non-test) release now that the pipeline is proven.
